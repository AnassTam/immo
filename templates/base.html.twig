{% extends 'index.html.twig' %}

{% block body %}

 <div class="container" style="width: auto">
     {% for label, messages in app.flashes %}
     {% for message in messages %}
         <div class="alert alert-{{ label }} -m4">
             {{ message }}
         </div>

     {% endfor %}
     {% endfor %}


      {% block  page_base %}
      {% endblock %}
  </div>

{% endblock %}